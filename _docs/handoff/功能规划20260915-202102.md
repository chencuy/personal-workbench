---
Anchor-Commit: ed11920a423eb5378321fd0ee05d8a220aa5c22a
Anchor-Branch: main
Worktree-Status: dirty
Created-At: 2026-09-15 20:21:02 +08:00
---

请接手下面这个已经进行过一段时间的任务。你的第一步只是理解上下文并复述。允许只读读取本交接文件；除此之外，在用户确认前不要修改文件、运行命令、调用工具或执行任何推进动作。

## 当前目标
- 继续开发本机个人工作台，并优先等待用户确认本轮“多语言、恢复初始状态、失败反馈、服务状态面板”的手动检查结果。
- 将下列明确列为后续待办，尚未获准实现：可配置概述仪表盘、番茄钟历史/完成提醒/今日专注统计、全局搜索扩展、提示词模板增强。

## 成功标准
- 已完成的服务与设置功能不破坏现有本地工作区数据、端口切换或静默启动流程。
- 后续功能按用户确认的范围实现，数据仅保存在本机，界面继续完整支持中文、English、日本語。
- 概述页最终可由用户配置仪表盘内容；番茄钟可提供历史、完成提醒和当日专注汇总。
- 全局搜索最终能够检索网址、书籍和 API Key 名称；提示词最终支持变量、收藏和版本历史。

## 用户明确要求 / 偏好
- 使用中文交流，输出简洁直接。
- 除非用户再次明确授权，不运行 `npm run build`、测试、浏览器自动化、截图或其他主动验证；完成改动后由用户自行检查。
- 不要自行启动额外开发服务、创建测试工作区、填写测试密码或插入测试数据。
- 不要删除、清空、导出或上传 `.workbench-data`；只有用户在界面内明确点击“恢复初始状态”并二次确认时才允许清理。
- 服务只能绑定 `127.0.0.1`；不要自行改变端口、开机自启动或 Windows 启动项。
- 未获明确请求时，不提交、不推送、不打包、不创建 Release。
- 不覆盖用户已有未提交改动。

## 已确认上下文
- 项目是 React + Vite 的纯本机个人工作台；默认地址为 `http://127.0.0.1:5180/`，端口可保存到项目本地数据目录。
- 工作区和业务数据在 `.workbench-data/`，该目录被 Git 忽略，禁止上传或主动清理。
- 本轮开始前已有提示词、网址收藏、API Key、应用快捷方式、本地书库、工作区与密码、导入导出、多语言、番茄钟、概述页和本地服务控制等模块。
- 当前分支为 `main`，工作区已有未提交改动；这些改动不能被回退或覆盖。

## 已完成工作
- `vite.config.js` 新增本地服务 API：`GET /api/server/status`、`GET /api/server/logs`、`POST /api/server/restart`、`POST /api/server/reset`。
- 服务重置接口会在前端确认后清除项目本地工作台数据、关闭开机自启动、恢复端口 `5180`，再静默重启。该行为尚未由用户手动验收。
- 设置页新增服务状态面板：运行状态、端口、PID、运行时长、刷新、重启、查看日志。
- 设置页数据管理新增“恢复初始状态”，含二次确认、处理中状态与失败反馈；开机自启动设置也加入失败反馈。
- 补齐多语言覆盖：登录/创建工作区、书籍阅读器、番茄钟错误与无障碍标签、分页、应用启动器反馈及常用提示。
- `BookReader` 已接收并使用 `language`；`SecurityGate` 明确传入已存语言。
- 已使用静默 VBS 入口尝试启动测试版，预期地址为 `http://127.0.0.1:5180/`；未确认实际是否成功运行。

## 当前状态
- 当前停在用户手动验收前，最近的用户请求仅要求把新功能需求写入待办，未要求现在实现。
- 未验证本轮代码的语法、构建、接口行为、页面表现或服务实际运行状态。
- 当前修改文件：`restart-workbench.cmd`、`src/main.jsx`、`src/styles.css`、`vite.config.js`。
- `restart-workbench.cmd` 的变更属于已有功能，应保留并与本轮变更共存。

## 验证命令 / 结果
- 已运行：`git rev-parse HEAD`、`git branch --show-current`、`git status --short`。
- 结果：提交为 `ed11920a423eb5378321fd0ee05d8a220aa5c22a`，分支为 `main`，工作区为 dirty；见“当前状态”的文件列表。
- 未运行 / 待验证：`npm run build`、任何测试、浏览器自动化、页面截图、服务 API 请求和实际端口访问；原因是用户明确要求不主动验证。

## 现实校准 / Git Anchor
- 本交接基于提交：`ed11920a423eb5378321fd0ee05d8a220aa5c22a`。
- 本交接生成时分支：`main`。
- 本交接生成时工作区状态：dirty。
- 用户确认继续后，先运行 `git status --short`。
- 如果 `Anchor-Commit` 不为 `Unknown` 且当前提交不同，先查看 `git diff --stat ed11920a423eb5378321fd0ee05d8a220aa5c22a`；只对本任务相关文件读取 scoped diff 或真实文件内容，不要盲目信任本交接里的代码现状描述。

## 相关文件 / 目录 / 工具
- `HANDOFF.md`：旧版项目交接与长期约束说明。
- `src/main.jsx`：设置界面、翻译、服务面板、恢复初始状态前端调用，以及既有应用模块。
- `src/styles.css`：服务面板、日志、危险操作和响应式样式。
- `vite.config.js`：本地服务 API、端口与数据目录逻辑。
- `restart-workbench.cmd`、`restart-workbench.vbs`、`start.cmd`、`end.cmd`、`end-workbench.vbs`：Windows 静默启停逻辑。
- `.workbench-data/`：用户本地数据，禁止读取敏感值、提交、上传或主动清除。
- `_docs/handoff/功能规划20260915-202102.md`：本交接文件。

## 已做决策
- 服务管理复用已有 Vite 配置和静默 VBS/CMD 启停机制，不引入新的服务守护进程。
- “恢复初始状态”定位为破坏性操作，必须在前端二次确认后才可触发，服务端仅接受本机请求。
- 多语言只翻译界面文本，不翻译用户录入的内容。
- 本次新需求只进入待办，未开始改动产品功能。

## 不要重复的尝试
- 不要用 `npm run build`、测试或浏览器操作确认本轮状态，除非用户之后明确授权。
- 不要删除 `.workbench-data` 以模拟全新安装，也不要把本地数据带入 Git 或发布包。
- 不要把端口重新硬编码到 `package.json`，端口必须继续由本地配置决定。
- 不要让服务监听 `0.0.0.0` 或任何公网地址。
- 不要自动创建、修改或删除 Windows 开机自启动项。
- 不要回退当前未提交的 `restart-workbench.cmd`、`src/main.jsx`、`src/styles.css`、`vite.config.js` 改动。

## 未决问题
- 用户尚未反馈本轮多语言、恢复初始状态、失败反馈和服务状态面板的手动验收结果。
- “可配置仪表盘”可配置的内容、布局方式和持久化范围尚未定义。
- 番茄钟完成提醒是仅页面内提醒、浏览器通知，还是两者兼具，尚未定义；使用浏览器通知通常需要用户授权。
- “今日专注统计”的统计口径尚未定义，例如只统计完成的番茄钟还是包含已开始/暂停的会话。
- 全局搜索对书籍的预期范围未定义：书名/元数据搜索已可规划；全文搜索对 PDF/EPUB 的实现成本与依赖需要先确认。
- API Key 搜索只能匹配名称等非敏感元数据，绝不能搜索或展示 Key 值。
- 提示词变量的语法、变量填充入口、版本保留数量与恢复规则尚未定义。

## 待办：用户刚提出的功能
- 概述页改为可配置仪表盘。
- 番茄钟增加历史记录、完成提醒、今日专注统计。
- 全局搜索扩展至网址、书籍、API Key 名称。
- 提示词模板增加变量、收藏、版本历史。

## 建议下一步
1. 等待用户先手动检查本轮设置与服务功能；收到反馈后仅针对反馈修正。
2. 用户确认开始新功能后，先读取相关真实代码和现有数据模型，再向用户确认未决产品规则，尤其是仪表盘布局、提醒方式、统计口径、书籍搜索范围和版本历史策略。
3. 将新功能拆分为独立小批次，优先定义本地数据迁移和多语言文案，再实现 UI 与交互。
4. 只有用户明确授权时，才运行构建、测试或浏览器验证；只有用户明确要求时，才打包、提交或推送。

## 给新会话的要求
- 允许只读读取本交接文件，并用简短文字复述你对任务的理解。
- 除读取本交接文件外，在用户确认前不要修改文件、运行命令、调用工具或执行任何推进动作。
- 用户确认继续后，先校验 Git 状态和本交接提到的关键文件；以真实代码和 Git diff 为准，本交接只作为任务理解入口。
- 明确区分事实、假设和建议。
- 不要扩大范围，不要补做用户没要求的功能。
